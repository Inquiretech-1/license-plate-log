/**
 * Domain models / shared shapes for API routes (extend as you add persistence).
 * @typedef {{ id: string, plate: string, when: string, location?: string, notes?: string }} LogEntry
 */

module.exports = {};
